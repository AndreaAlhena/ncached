{
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "description": "ng-cached is as a multi layer cache for Angular applications. Its purpose is simple: allowing developers to create a layered (or multi-level) cache in a breeze.",
  "devDependencies": {
    "husky": "^9.1.7",
    "puppeteer": "^23.11.1"
  },
  "keywords": ["Angular", "cache", "ncached", "ng-ncached"],
  "name": "ng-ncached",
  "peerDependencies": {
    "@angular/common": ">=13.0.0",
    "@angular/core": ">=13.0.0"
  },
  "scripts": {
    "prepare": "husky",
    "test": "ng test ncached --karma-config ./projects/ncached/karma.conf.js --progress --no-watch"
  },
  "sideEffects": false,
  "version": "0.1.3"
}
